{% extends 'base.html.twig' %}

{% block body %}
    {# Hello {{ prenom }}, tu as {{ age }} ans !#}
     <ul>
    {% for prenom in tableau_prenoms %}
        <li>{{ prenom }}</li>
    {% endfor %}
	</ul> 
	
	{{ message }}
	{# tabDisp|raw #}
	{{ tabDisp(tableau) }}
	

{% endblock %}


{# commentaire #}
